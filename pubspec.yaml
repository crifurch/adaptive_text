name: adaptive_text
description: Package that contains text widgets that can auto size by provided constrains
version: 0.0.1
homepage:

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
